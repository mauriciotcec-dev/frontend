Pequeño frontend Flask que consume backend-api. Ajustar BACKEND_API_URL con el service name del cluster (por ejemplo, http://backend-api:5000).

Workflow configurado para Docker Hub. Añade DOCKERHUB_USERNAME y DOCKERHUB_TOKEN como Secrets.
